<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Declare that telephony is used, but not strictly required -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <queries>
        <!-- Allow querying all apps with launcher activities (for whitelist selection) -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        
        <!-- Essential system apps -->
        <package android:name="com.android.phone"/>  <!-- Calls -->
        <package android:name="com.google.android.dialer"/>
        <package android:name="com.android.mms"/>  <!-- SMS -->
        <package android:name="com.google.android.apps.messaging"/>
        <package android:name="com.android.deskclock"/>  <!-- Clock -->
        <package android:name="com.google.android.deskclock"/>
        <package android:name="com.sec.android.app.clockpackage"/>  <!-- Samsung Clock -->
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="ZenLock"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:enableOnBackInvokedCallback="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZenLock"
        tools:targetApi="31">


        <!-- Main Activity (App Launch) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lock Screen Activity -->
        <activity
            android:name=".LockScreenActivity"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <!-- Whitelist Activity -->
        <activity
            android:name=".WhitelistActivity"
            android:exported="true"
            android:theme="@style/Theme.ZenLock" />

        <!-- Partner Contact Activity -->
        <activity
            android:name=".PartnerContactActivity"
            android:exported="false"
            android:theme="@style/Theme.ZenLock" />

        <!-- Accessibility Service for App Blocking -->
        <service
            android:name=".AppBlockerService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
        </service>

        <!-- Boot Receiver (Runs on Device Restart) -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- SMS Status Receivers -->
        <receiver
            android:name=".utils.OTPManager$SmsSentReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="zenlock.SMS_SENT_ACTION" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".utils.OTPManager$SmsDeliveredReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="zenlock.SMS_DELIVERED_ACTION" />
            </intent-filter>
        </receiver>

    </application>



</manifest>