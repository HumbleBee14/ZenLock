<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="24dp"
    android:background="@color/backgroundPrimary">

    <!-- Title -->
    <TextView
        android:id="@+id/disclosureTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="SMS Permission Consent"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/textPrimary"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Icon -->
    <ImageView
        android:id="@+id/disclosureIcon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@android:drawable/ic_dialog_email"
        android:contentDescription="SMS Permission"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/disclosureTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Scrollable Content -->
    <ScrollView
        android:id="@+id/scrollContent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:maxHeight="300dp"
        android:layout_marginBottom="24dp"
        app:layout_constraintTop_toBottomOf="@id/disclosureIcon"
        app:layout_constraintBottom_toTopOf="@id/buttonContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Main disclosure text -->
            <TextView
                android:id="@+id/disclosureText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="ZenLock needs SMS permission to send unlock codes (OTP) to your accountability partner.\n\nHow it's used:\n• Send OTP when you request early session unlock\n• Only sent when you explicitly request it\n\nWe DO NOT:\n• Read your messages\n• Collect or share any data\n\nBy agreeing, you consent to ZenLock sending SMS only for unlock verification."
                android:textSize="14sp"
                android:textColor="@color/textPrimary"
                android:lineSpacingExtra="4dp" />

        </LinearLayout>

    </ScrollView>

    <!-- Buttons -->
    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button
            android:id="@+id/declineButton"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:text="Decline"
            android:textColor="@color/textPrimary"
            app:strokeColor="@color/textSecondary" />

        <Button
            android:id="@+id/agreeButton"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="I Agree"
            android:textSize="16sp"
            android:textStyle="bold" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
